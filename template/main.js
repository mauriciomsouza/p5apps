function setup() {
	createCanvas(windowWidth, windowHeight);
	b1 = color(255, 25, 95);
	background(b1);
}

function draw() {
	fill(255);
	textSize(36);
	textAlign(CENTER);
	text('p5.js PWA WebApp Template', windowWidth/2, windowHeight/2);
}